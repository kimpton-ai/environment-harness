# HTTP 0.2 → 0.3 migration

<!-- Generated by scripts/build_http_migration.py. Do not edit by hand. -->

Every operation published by `environment-harness==0.2.4rc2` is classified exactly once below. The
machine-readable source is [`contracts/migrations/http-0.2-to-0.3.json`](../contracts/migrations/http-0.2-to-0.3.json),
and CI regenerates this page so documentation cannot drift from enforcement.

Consumers such as EvalRouter must pin `environment-harness==0.2.4rc2` until their adapter is migrated.

## Route and contract changes

| Old operation | New operation | Disposition | Breaking dimensions | Notes |
| --- | --- | --- | --- | --- |
| `POST /v1/environments/{environment}/actions` | `POST /v1/sessions/{session_id}/participants/{participant_id}/actions` | mapped | route, access_policy, resource_constraints | Only a participant credential bound to this session and participant may submit. |
| `GET /v1/activity/snapshot` | `GET /v1/activity/hierarchy` | mapped | route, access_policy | `snapshot` is the name of the immutable TrajectorySnapshot resource, so the activity projection is renamed `hierarchy`. `ActivitySnapshot` becomes `ActivityHierarchy`. |
| `GET /v1/environments/{environment}/agent-work` | `GET /v1/sessions/{session_id}/invocations` | mapped | route, response, access_policy | Hyphenated `agent-work` becomes `invocations`; the body key becomes `items`. |
| `POST /v1/environments/{environment}/artifacts` | `POST /v1/sessions/{session_id}/artifacts` | mapped | route, access_policy | Route rename only. Oversize bodies now return the payload_too_large code. |
| `POST /v1/environments/{environment}/commands` | `POST /v1/sessions/{session_id}/commands` | mapped | route, response, access_policy | Queued lifecycle commands return 202 with a typed receipt whose result carries the previous body. |
| `POST /v1/compare` | `POST /v1/comparisons` | mapped | route, request, access_policy | The verb path becomes the plural resource, and the request key `environments` becomes `sessions`. No comparison authority is persisted. |
| `POST /v1/environments` | `POST /v1/experiments` | mapped | route, response, access_policy | Every Session is derived from an Experiment, so the supported one-off path creates an implicit experiment-of-one. The response now returns 201 with a Location header. |
| `POST /v1/environments/{environment}/credentials` | `POST /v1/sessions/{session_id}/participants/{participant_id}/credentials` | mapped | route, request, access_policy, resource_constraints | The participant is named by the route, never the body, and the operation declares the participant-credentials capability. |
| `GET /v1/environments/{environment}/activity` | `GET /v1/sessions/{session_id}/activity` | mapped | route, access_policy | Route rename only; the durable activity cursor is preserved. |
| `GET /v1/environments/{environment}/turn-series` | `GET /v1/sessions/{session_id}/turn-series` | mapped | route, access_policy | Route rename only. |
| `GET /v1/environment` | — removed | removed | route, request, response, errors, access_policy, resource_constraints | There is no Environment singleton. EnvironmentSpec is frozen inside each Experiment and inherited by its Sessions; read it from Experiment.spec.environment or Session.spec.environment. |
| `GET /v1/environments` | `GET /v1/sessions` | mapped | route, response, pagination, access_policy | The bare list is replaced by the typed management envelope with an opaque cursor, and items are portable Session resources. |
| `GET /v1/environments/{environment}/events` | `GET /v1/sessions/{session_id}/evidence` | mapped | route, access_policy | Renamed to `evidence` and given a typed EvidencePage response; the durable integer cursor and SSE resume are unchanged. |
| `GET /v1/experiments/{experiment}/events` | `GET /v1/experiments/{experiment_id}/activity` | mapped | route, access_policy | Experiment activity is scoped consistently at `/activity`. |
| `GET /v1/trajectory-datasets/{dataset}/export` | `GET /v1/datasets/{dataset_id}/records` | mapped | route, response, pagination, access_policy | Content negotiation replaces the `/export` verb path. |
| `GET /v1/trajectory-snapshots/{snapshot}/export` | `GET /v1/snapshots/{snapshot_id}/records` | mapped | route, response, pagination, access_policy | Content negotiation replaces the `/export` verb path: the typed JSON cursor page is the generated-client response and `Accept: application/x-ndjson` streams NDJSON. |
| `GET /v1/environments/{environment}/export` | — removed | removed | route, request, response, pagination, errors, access_policy, resource_constraints | The action-row `environment-rollout.v1` export is corrected once before 0.3.0rc1. Freeze a TrajectorySnapshot and read `/v1/snapshots/{id}/records`, or freeze a TrajectoryDataset and read `/v1/datasets/{id}/records`, with `Accept: application/x-ndjson` for streaming. Stored evidence stays readable through the trajectory projection. |
| `POST /v1/trajectory-datasets` | `POST /v1/datasets` | mapped | route, access_policy | One-word plural collection name; creation returns 201 with Location. |
| `POST /v1/trajectories/{trajectory}/snapshots` | `POST /v1/trajectories/{trajectory_id}/snapshots` | mapped | access_policy | Creation now returns 201 with a Location header for the frozen snapshot. |
| `GET /v1/environments/{environment}` | `GET /v1/sessions/{session_id}` | mapped | route, response, access_policy | The response is the portable Session resource with independent status dimensions. |
| `GET /v1/activity/events` | `GET /v1/activity` | mapped | route, access_policy | `/v1/activity/events` becomes the collection itself at `/v1/activity`. |
| `GET /health` | `GET /health` | retained | none | Operational health is unauthenticated and unchanged. |
| `POST /v1/trajectory-sources/{source}/records` | `POST /v1/sources/{source_id}/records` | mapped | route, errors, access_policy | Route rename plus the declared historical-ingestion capability. |
| `GET /v1/environments/{environment}/observation` | `GET /v1/sessions/{session_id}/participants/{participant_id}/observation` | mapped | route, request, access_policy, resource_constraints | Participant scope moves from a query parameter into genuine path ownership. |
| `POST /v1/environments/{environment}/operations` | `POST /v1/sessions/{session_id}/operations` | mapped | route, access_policy | Route rename only; operation preparation remains a participant operation. |
| `GET /v1/environments/{environment}/artifacts/{key}` | `GET /v1/sessions/{session_id}/artifacts/{artifact_id}` | mapped | route, access_policy | Route rename and a named `artifact_id` path parameter. |
| `POST /v1/trajectory-sources` | `POST /v1/sources` | mapped | route, errors, access_policy | One-word plural collection name, 201 with Location, and a declared historical-ingestion capability that returns 501 when disabled instead of hiding the route. |
| `POST /v1/environments/{environment}/reports` | `POST /v1/sessions/{session_id}/scores` | mapped | route, access_policy | Publishing a report returns 201 with Location and an ETag on the stored hash. |
| `GET /v1/environments/{environment}/reports` | `GET /v1/sessions/{session_id}/scores` | mapped | route, response, access_policy | ScoreReport is returned through contextual `/scores` collections. The Python contract keeps its name; the body key becomes `items`. |
| `GET /v1/training-runs/{training_run}` | `GET /v1/training-runs/{training_run_id}` | mapped | access_policy | Path parameter rename only. |
| `GET /v1/training-runs` | `GET /v1/training-runs` | mapped | response, pagination, access_policy | The bare list becomes the typed management envelope. |
| `GET /v1/trajectory-datasets/{dataset}` | `GET /v1/datasets/{dataset_id}` | mapped | route, access_policy | One-word plural collection name plus an ETag on the dataset digest. |
| `GET /v1/trajectory-datasets` | `GET /v1/datasets` | mapped | route, response, pagination, access_policy | One-word plural collection name plus the typed management envelope. |
| `GET /v1/trajectories` | `GET /v1/trajectories` | mapped | response, pagination, access_policy | The bare list becomes the typed management envelope. |
| `GET /v1/trajectories/{trajectory}/records` | `GET /v1/trajectories/{trajectory_id}/records` | mapped | access_policy | Path parameter rename only; the durable integer sequence cursor is preserved. |
| `GET /v1/trajectories/{trajectory}` | `GET /v1/trajectories/{trajectory_id}` | mapped | response, access_policy | `status.records` is removed. Read records through `/v1/trajectories/{id}/records`; status now carries segments, a record count, the sequence boundary, and typed collection health. |
| `GET /v1/trajectory-snapshots/{snapshot}` | `GET /v1/snapshots/{snapshot_id}` | mapped | route, access_policy | One-word plural collection name plus an ETag on the snapshot digest. |
| `GET /v1/trajectory-snapshots` | `GET /v1/trajectories/{trajectory_id}/snapshots` | mapped | route, request, response, pagination, access_policy | Listing a trajectory's snapshots is nested under its owner; `/v1/snapshots` remains a top-level index because a snapshot is independently addressable. |
| `GET /v1/trajectory-sources/{source}/status` | `GET /v1/sources/{source_id}/status` | mapped | route, access_policy | One-word plural collection name. |
| `PUT /v1/trajectory-sources/{source}/status` | `POST /v1/sources/{source_id}/status-reports` | mapped | route, errors, access_policy | Declaring collection health is an appended status report, not an idempotent PUT, so the operation becomes `POST /v1/sources/{id}/status-reports` and returns 201. |

## Authorization changes

The public four-role model is removed. A caller sends only an opaque bearer credential and
the server resolves it to one of these fixed policies. Requests never assert a policy,
role, or permission. See [Authentication](AUTHENTICATION.md).

| Credential policy | Issuable | Registered actions |
| --- | --- | --- |
| `management` | yes | 22 |
| `participant` | yes | 9 |
| `trusted-local` | no | 27 |
| `viewer` | yes | 11 |

Authorization-registry fingerprint: `sha256:07ffb8d6-489e1f56-e824cf1e-8850c72a-fd6ffdfb-9237a6b4-c12e4cf6-8a5a709a`.

Every old operation's authorization behavior changed, because the roles it accepted no
longer exist. All pre-`0.3.0rc1` credentials are deleted by migration
`005_credential_policies` and must be reissued; old bearer tokens return `401`.

## Operations outside the public hierarchy

| Route | Why it is separate |
| --- | --- |
| `/local/connect` | loopback viewer handshake on the operational routes |
| `/v1/worker/call` | separate create_worker_app process on environment-worker.v1 |
| `/viewer/config` | operational viewer configuration |

## Operations added in 0.3.0rc1

These have no 0.2 predecessor.

| Operation |
| --- |
| `POST /v1/sessions/{session_id}/branches` |
| `POST /v1/sessions/{session_id}/checkpoints` |
| `GET /v1/capabilities` |
| `GET /v1/experiments/{experiment_id}/scores` |
| `GET /v1/experiments/{experiment_id}/sessions` |
| `GET /v1/experiments/{experiment_id}` |
| `GET /v1/experiments` |
| `GET /v1/sessions/{session_id}/checkpoints/{checkpoint_id}` |
| `GET /v1/policies` |
| `GET /v1/policies/{policy_id}` |
| `GET /v1/scenario-sets/{scenario_set_id}` |
| `GET /v1/scenario-sets` |
| `GET /v1/sessions/{session_id}/checkpoints` |
| `GET /v1/snapshots` |
| `GET /v1/sources/{source_id}/records` |
| `GET /v1/sources/{source_id}` |
| `GET /v1/sources` |
| `GET /v1/trajectories/{trajectory_id}/scores` |
