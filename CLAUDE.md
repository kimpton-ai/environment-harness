# Repository instructions

Follow the project-wide instructions in `AGENTS.md`.

## Branch names

The authoritative Conventional Branch policy is the **Branch names** section of `AGENTS.md`. Apply it when creating, suggesting, validating, or—when explicitly requested—renaming a branch. Do not replace its purpose-based project policy with agent-specific prefixes.
